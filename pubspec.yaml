name: easy_dev_toolkit
description: A mixed Material/iOS clean UI toolkit with responsive sizing, adaptive widgets, network helpers, layouts, and utilities.
version: 0.0.1
homepage: https://github.com/yourname/easy_dev_toolkit

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.0.0
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
