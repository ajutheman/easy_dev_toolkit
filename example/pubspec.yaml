name: example
description: Example app for easy_dev_toolkit
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  easy_dev_toolkit:
    path: /Users/ajmalm/projectfile/package/easy_dev_toolkit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
